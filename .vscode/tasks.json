{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Run Zig Build",
            "type": "shell",
            "command": "zig build run",
            "group": {
                "kind": "build",
                "isDefault": true
            },
            "problemMatcher": [],
            "detail": "Run the zig build run command"
        }
    ]
}